# UAT Report — Task #77 — 2026-05-02 22:00 IST

**Result**: PARTIAL (AC1–AC3 PASS; AC4 BLOCKED on Inno Setup)
**Skill version**: uat-task @ 2026-05-02
**Learnings applied**: 9
**--allow-prod**: no
**Total duration**: ~30s

## Summary
- ACs passed: 3/4
- ACs failed: none
- ACs blocked: AC4 — Inno Setup 6 not installed on this machine; matches Task #68 partial deferral of installer build to v1.0.0 release tag
- ACs skipped: none

## Screenshot index

| # | File | AC | Caption |
|---|------|----|---------|
| 1 | data/uat/screenshots/task77_ac1_icon_256.png | AC1 | Extracted 256×256 frame from icon.ico — blue disc with white "A" glyph |
| 2 | data/uat/screenshots/task77_ac2_icon_32.png  | AC2 | 32×32 frame extracted from the multi-size ICO |
| 3 | data/uat/screenshots/task77_ac2_icon_16.png  | AC2 | 16×16 frame extracted from the multi-size ICO |

## ACs

### AC1 — `icon.ico` exists and is a valid ICO file — PASS
- **Action**: `python -c "from PIL import Image; img=Image.open('scripts/build/installer/icon.ico'); assert img.format=='ICO'"`
- **Expected**: format == ICO; file size > 5KB
- **Actual**: format=ICO, default_size=(256,256), size=44,258 bytes (well above 5KB minimum)
- **Evidence**:
  - File: `scripts/build/installer/icon.ico` (44 KB)
  - Generator: `scripts/build/installer/generate_icon.py` (procedural, deterministic, re-runnable)
  - **Screenshot**:
    ![AC1 — 256px icon](../../data/uat/screenshots/task77_ac1_icon_256.png)
- **Duration**: <1s

### AC2 — ICO contains all 6 required sizes (16/32/48/64/128/256 px) — PASS
- **Action**: `python -c "from PIL.IcoImagePlugin import IcoFile; sizes=IcoFile(open('scripts/build/installer/icon.ico','rb')).sizes(); ..."`
- **Expected**: set of frame sizes includes (16,16), (32,32), (48,48), (64,64), (128,128), (256,256)
- **Actual**: All 6 present: `[(16,16), (32,32), (48,48), (64,64), (128,128), (256,256)]`
- **Evidence**:
  ```
  PASS all 6 sizes present: [(16, 16), (32, 32), (48, 48), (64, 64), (128, 128), (256, 256)]
  ```
  - **Screenshots**:
    ![AC2 — 32px frame](../../data/uat/screenshots/task77_ac2_icon_32.png)
    ![AC2 — 16px frame](../../data/uat/screenshots/task77_ac2_icon_16.png)
- **Note on spec edit**: AC2's original Action used `getattr(img,'n_frames',1)` which returns 1 for ICO — Pillow's high-level Image API does NOT expose ICO frames that way. Updated AC2 to use `IcoImagePlugin.IcoFile.sizes()` which is the correct API. Spec edit committed alongside this report.
- **Duration**: <1s

### AC3 — `ICON_PLACEHOLDER.md` is deleted — PASS
- **Action**: `python -c "import os; assert not os.path.exists('scripts/build/installer/ICON_PLACEHOLDER.md')"`
- **Expected**: File no longer exists
- **Actual**: `EXISTS: False` — placeholder removed in this UAT run after icon.ico generation succeeded
- **Evidence**: `ls scripts/build/installer/` shows only `eula.rtf, generate_icon.py, icon.ico, postinstall.sh, windows.iss` — placeholder gone
- **Duration**: <1s

### AC4 — Installer build runs cleanly + installer shows correct icon — BLOCKED
- **Action**: `powershell scripts/build/build_windows_installer.ps1` then File Explorer Properties visual check
- **Expected**: Inno Setup compiles installer.exe with embedded icon
- **Actual**: BLOCKED — Inno Setup 6 not installed on this machine. `Test-Path 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe'` → False. `Test-Path 'C:\Program Files\Inno Setup 6\ISCC.exe'` → False.
- **Resolution path**: This AC was already deferred under Task #68 partial closure (2026-05-01). Per `docs/STATUS.md`: "Live verification deferred to v1.0.0 release tag". GHA build matrix (`.github/workflows/release.yml`) builds the installer in CI when the v1.0.0 tag is pushed. AC4 will be verified at that time on the GHA runner, which has Inno Setup pre-installable via Chocolatey.
- **Evidence**: PowerShell probe output (Test-Path × 2 = False)
- **Duration**: <1s

## Aggregated PASS rule for Task #77

1. ✅ AC1–AC3 all PASS (automated)
2. ⏸ AC4 BLOCKED — Inno Setup absent locally; deferred to v1.0.0 release tag per Task #68 partial closure
3. ✅ `ICON_PLACEHOLDER.md` confirmed deleted (AC3)
4. ✅ UAT report written with PIL output and all 3 size previews embedded

## Recommendation

**Mark Task #77 as PARTIAL — same status as parent Task #68**. AC1–AC3 are fully verified; AC4 (icon visible in installer Properties dialog) is blocked on local Inno Setup. The icon file itself is shipped and correct — at the v1.0.0 release tag the GHA runner will compile the installer and embed this icon, satisfying AC4 there.

Suggested status update: `task-master set-status --id=77 --status=partial` (or `done` if the user accepts AC4 as covered by the v1.0.0 GHA matrix).

## Notes

- Generator is procedural (`generate_icon.py`) not a binary blob — re-runnable, version-control-friendly, no design tool needed. Output is deterministic.
- Icon visual: blue gradient disc (#2563eb → #1d4ed8, brand colors from Tailwind blue-600/700), white "A" glyph rendered as triangle + crossbar with stroke proportional to size. Crisp at 16px (no muddy downsample) thanks to 4x oversample render before downsample.
- Spec AC2 fixed from the broken `n_frames` API to `IcoImagePlugin.IcoFile.sizes()` — appended to LEARNINGS would be useful but the fix is in the spec itself, so future runs will use the correct check.
