# UAT Report — Task #85 — 2026-05-02 22:45 IST

**Result**: PASS (6/6 ACs)
**Skill version**: uat-task @ 2026-05-02
**Learnings applied**: 9
**--allow-prod**: no
**Total duration**: ~30s

## Summary
- ACs passed: 6/6
- ACs failed: none
- ACs blocked: none
- ACs skipped: none

## Screenshot index

(N/A — Task #85 is a CLI / DB cleanup helper; no UI surface to screenshot. Evidence is row-count diffs + stderr / exit codes.)

## ACs

### AC1 — `--target local` deletes UAT user + every child row — PASS
- **Action**: seed → `python scripts/uat/cleanup_test_user.py --email uat-task85-user@pointcapitalis.com`
- **Expected**: exit 0; per-table cleanup counts; post-state zero rows
- **Actual**: cleanup output:
  ```
  Found user id=1 email=uat-task85-user@pointcapitalis.com target=local
  Deleted rows:
    metrics: 2
    posts: 1
    campaign_assignments: 1
    campaign_invitation_log: 1
    payouts: 1
    penalties: 1
    agent_status: 1
    agent_commands: 1
    drafts: 1
    users: 1
  User 'uat-task85-user@pointcapitalis.com' successfully removed.
  ```
  Post-state SQL: every row count = 0 across all 9 child tables + the user row itself.
- **Duration**: ~2s

### AC2 — `--target prod` requires `PROD_DATABASE_URL` — PASS
- **Action**: `python scripts/uat/cleanup_test_user.py --email uat-task85-user@pointcapitalis.com --target prod` (env unset)
- **Expected**: exit non-zero; helpful error naming `PROD_DATABASE_URL`; no DB writes
- **Actual**:
  ```
  ERROR: --target prod requires the PROD_DATABASE_URL env var.
  Set it to the Supabase pooler URL before running, e.g.:
    export PROD_DATABASE_URL='postgresql+asyncpg://...@aws-1-us-east-1.pooler.supabase.com:6543/postgres'
  ```
  exit 2.
- **Duration**: <1s

### AC3 — Cascade covers `campaign_invitation_log` — PASS
- **Action**: covered by AC1 — seed creates 1 invitation_log row, cleanup deletes it
- **Expected**: pre-state count=1, post-state count=0, cleanup stdout reports `campaign_invitation_log: 1`
- **Actual**: pre `campaign_invitation_log: 1` → post `campaign_invitation_log: 0`. Cleanup output: `campaign_invitation_log: 1` reported.
- **Note**: This was the explicit gap that triggered Task #85 — orphan invitation_log rows were leaking into prod metrics after every UAT teardown.
- **Duration**: included in AC1 wall-clock

### AC4 — Cascade covers all 7 other child tables — PASS
- **Action**: pre/post SQL counts on `campaign_assignments`, `posts`, `metrics`, `payouts`, `penalties`, `agent_status`, `agent_commands`, `drafts`
- **Expected**: each pre count = N>0, each post count = 0
- **Actual**:
  | Table | Pre | Post |
  |---|---|---|
  | campaign_assignments | 1 | 0 |
  | payouts              | 1 | 0 |
  | penalties            | 1 | 0 |
  | agent_status         | 1 | 0 |
  | agent_commands       | 1 | 0 |
  | drafts               | 1 | 0 |
  | posts                | 1 | 0 |
  | metrics              | 2 | 0 |
- **Duration**: included in AC1

### AC5 — Safety guard refuses non-UAT-pattern emails — PASS
- **Action**: `python scripts/uat/cleanup_test_user.py --email real-user@gmail.com`
- **Expected**: exit non-zero; pattern-mismatch error; no DB writes
- **Actual**:
  ```
  ERROR: 'real-user@gmail.com' does not match uat-task<n>*@pointcapitalis.com — refusing to delete.
  ```
  exit 3.
- **Duration**: <1s

### AC6 — Idempotent — PASS
- **Action**: run cleanup on already-deleted email
- **Expected**: exit 0; no error; "nothing to delete" message
- **Actual**:
  ```
  No user found with email 'uat-task85-user@pointcapitalis.com' — nothing to delete.
  ```
  exit 0.
- **Duration**: <1s

## Aggregated PASS rule

1. ✅ AC1–AC6 all PASS
2. ✅ UAT report written
3. ✅ `scripts/uat/seed_cleanup_fixture.py` exists and is reusable — auto-creates schema on local SQLite, idempotent re-seeding (deletes pre-existing test user)

## Recommendation

**All checks pass — safe to mark Task #85 done.** Run: `task-master set-status --id=85 --status=done`.

## Notes

- Two new files: `scripts/uat/seed_cleanup_fixture.py` (synthetic seeder, ~165 LOC, no AI / no platform calls) and `docs/specs/cleanup-cascade.md` (AC block).
- Cleanup script changes: added `CampaignInvitationLog` to the cascade chain (was missing, the actual bug); added `--target {local,prod}` flag; tightened safety-guard exit codes (2 = missing env, 3 = pattern violation, 0 = success/no-op).
- Iteration cost: 6 model-field correction rounds during fixture authoring (User.audience_region not region, Company.name not company_name, Campaign.budget_total NOT NULL, Campaign.start_date NOT NULL, Post.content_hash + posted_at NOT NULL, Metric.scraped_at NOT NULL, Payout.period_start + period_end NOT NULL, Metric.impressions not views). Captured here so the next time someone seeds synthetic fixtures they have the full required-field manifest.
