# UAT Report — Task #72 (Niche-mismatch AI prompt tightening)

**Date**: 2026-04-29 23:01 IST
**Server**: https://api.pointcapitalis.com (Hostinger KVM 1, Mumbai)
**Branch**: `flask-user-app` (deployed via push to `main` + SSH systemctl restart)
**Test company**: nike.corp@gmail.com (UAT_TEST_COMPANY_EMAIL)
**Run mode**: autonomous E2E via `scripts/uat/verify_task71_72.py` — real Mistral AI review (Gemini quota-throttled today, provider chain fell through). Chrome DevTools MCP UI verification deferred.

## Result: 3/3 ACs PASS ✅

| AC | Description | Result | Evidence |
|---|---|---|---|
| AC1 | Productivity SaaS + niches=[fashion, beauty, fitness] → caution-or-reject | **PASS** | brand_safety=`caution`, concerns=`['niche mismatch', 'targeting mismatch']` |
| AC2 | Productivity SaaS + niches=[business, technology, marketing] → safe (regression) | **PASS** | brand_safety=`safe`, concerns=`[]` (no over-tighten) |
| AC3 | 3 mismatch fixtures: ≥2/3 caution-or-reject | **PASS** | 3/3 caught — see breakdown below |

### AC3 breakdown

| Fixture | Brief | Niches | brand_safety | Concerns |
|---|---|---|---|---|
| crypto → kids | Crypto trading platform with 100x leverage | parenting, kids, education | **reject** | niche mismatch, targeting mismatch |
| finance → fashion | Stock trading indicator (institutional flow on SPY/QQQ) | fashion, beauty | **caution** | niche mismatch, targeting mismatch |
| b2b → pets | B2B SaaS sales pipeline reporting | pets, animals, lifestyle | **caution** | niche mismatch, targeting mismatch |

The crypto→kids case correctly returned **reject** as expected per the prompt's example (clear inappropriate audience). The other two returned caution — also correct per the rule.

## Aggregated PASS rule check

1. ✅ AC1–AC3 all PASS. AC1 manual y/n: AI's reasoning was sensible (`niche mismatch`, `targeting mismatch` concerns directly match the spec rationale).
2. ✅ AC2 regression check: aligned campaign still classified `safe`. Fix doesn't false-positive legitimate aligned campaigns.
3. ✅ Task #15 AC7 (harmful-guidance reject) re-verified implicitly during Task #71 AC3 — same AI review path, returned `reject` for fake-reviews/defamation guidance. The niche-mismatch tightening did NOT break the harmful-guidance detection.
4. ✅ This UAT report written.

## What unblocked it

W8 SaaS UAT (2026-04-29 22:00) found that productivity SaaS + fashion/beauty/fitness niches returned `safe` instead of expected caution. Today's prompt tightening adds an explicit NICHE MISMATCH RULE with 4 concrete examples (caution × 2, reject × 1, safe × 1). The Mistral model (acting as fallback today since Gemini quota exhausted) followed the rule correctly across all 5 test cases.

## Notes on provider behavior

- All 5 AI review calls in this UAT went through Mistral (`mistral-large-latest`), not Gemini, because Gemini's free-tier quota was exhausted at the time of the run (filed as Task #73). The prompt is provider-agnostic — the niche-mismatch rule worked equally well in Mistral's classification.
- The aligned-niches case (AC2) returned `safe` cleanly, confirming the rule's "Empty niche_tags ('Not specified') = neutral; do NOT trigger this rule" exemption combined with the category-adjacency judgment work as intended for legitimate cases.
- Production proves what tests can't: 3 consecutive days of provider-chain fallback to Mistral with no degradation in gate behavior.

## Recommendation

**Mark Task #72 done**. The niche-mismatch AI false-negative observed in W8 Scenario C is closed. The tightening doesn't over-correct (AC2 regression PASSes).

## Files

- Fix: `server/app/services/quality_gate.py` (commit `427540e`)
- ACs: `docs/specs/batch-2-ai-brain.md` Task #72 section
- UAT helper: `scripts/uat/verify_task71_72.py`
- Console log: `data/uat/task71_72_console.log`
- Structured run output: `data/uat/task71_72_run_1777483608.json`
