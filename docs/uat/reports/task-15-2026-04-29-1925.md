# UAT Report — Task #15 (AI Campaign Quality Gate) — 2026-04-29 19:25 IST

**Result**: **PASS** (14/14)
**Skill version**: uat-task @ 0e9f288 (current branch HEAD: 0e9f288)
**Learnings applied**: 8 (LEARNINGS.md — 6 prior + 2 added during 1545 run)
**--allow-prod**: yes (live api.pointcapitalis.com against UAT-prefixed test campaigns; with no real users on the platform yet)
**Server commit deployed**: 0e9f288 (Mistral+Groq fallback, retry, PATCH fix, hard-fail criteria, HTTP-header test-mode)
**Total duration**: ~5 min wall clock for the test loop (post-fix iteration)

## Summary

- ACs passed: **14/14**
- ACs failed: 0
- ACs blocked: 0
- ACs skipped: 0

This is the **first all-green run** of `/uat-task 15`. All 5 follow-ups from the 1545 partial report were addressed. The skipped ACs (AC12, AC13, AC14) became runnable once Supabase credentials were available and HTTP-header test-mode flags replaced server-restart env vars.

## Screenshot index

| # | File | AC | Caption |
|---|------|----|---------|
| 1 | `data/uat/screenshots/task15_ac12_v2_blocked.png` | AC12 | bad_minimal (id 47) — Quality Score 2/100 NEEDS WORK, Activate button disabled with "Score below 85" tooltip, all 8 criteria with feedback bullets |
| 2 | `data/uat/screenshots/task15_ac14_v2_before_activate.png` | AC14 | idempotence_check (id 55) after PATCH — Quality Score 87/100 PASS, Activate button enabled, all PATCHed nested fields visible (budget $150, payouts $1.50/$0.03/$0.08, end May 21) |
| 3 | `data/uat/screenshots/task15_ac14_v2_after_active.png` | AC14 | same campaign post-Activate — status badge "active", "Campaign active" success banner, Pause button replaces Activate |

## ACs

### AC1 — Bad campaign blocked: rubric < 50, specific feedback — PASS
- pytest `test_ac1_bad_campaign_blocked` PASSED
- HTTP 422, score 2/100, all 4 expected feedback substrings present, status stayed `draft`

### AC2 — Per-criterion score breakdown — PASS
- pytest `test_ac2_criterion_breakdown` PASSED
- All 8 keys present, scores sum to 2 (matches), correct zero scores on bad criteria

### AC3 — Wizard-quality campaign passes — PASS
- pytest `test_ac3_wizard_passes` PASSED
- HTTP 200, score 95+, status=active. AI review went through Mistral/Groq fallback chain (server logs show provider chain firing — Gemini still 503-throttled)

### AC4 — Fix-and-retry — PASS (after PATCH endpoint fix)
- pytest `test_ac4_fix_and_retry` PASSED
- Pre-fix: HTTP 422, post-PATCH all nested fields applied, post-fix: HTTP 200, score >= 85
- **Fix 3 verified**: PATCH now correctly stores `payout_rules`, `targeting`, `assets`, `budget_total`, dates

### AC5 — Zero-rates fails — PASS (after hard-fail criteria fix)
- pytest `test_ac5_zero_rates_fails` PASSED
- HTTP 422, criteria.payout_rates.score == 0
- **Hard-fail rule verified**: zero on payout_rates / assets / targeting now vetoes activation regardless of total score (zero_rates was at exactly 85 boundary before the fix)

### AC6 — Repost campaign exempt from guidance — PASS
- pytest `test_ac6_repost_exempt_from_guidance` PASSED
- HTTP 200, content_guidance got full score with "exempt — repost campaign" feedback

### AC7 — Brand-safety reject — PASS (after provider fallback fix)
- pytest `test_ac7_brand_safety_reject` PASSED
- AI review caught harmful guidance, returned `brand_safety='reject'`, HTTP 422
- **Fix 1+1b verified**: Gemini → Gemini-fallback-model → Mistral → Groq chain. Prior run failed because Gemini was 503; this run succeeded because Mistral picked up.

### AC8 — Brand-safety caution branch — PASS (after HTTP-header test-mode fix)
- pytest `test_ac8_caution_flag` PASSED
- Sent `X-Amplifier-UAT-Force-AI-Review-Result` header with `{"brand_safety":"caution",...}`. Campaign activated, audit_log got `campaign_flagged_caution` row, admin_review_queue got entry.
- **Fix 5 verified**: HTTP-header alternative works without server restart, header is honored only for the UAT test company

### AC9 — Targeting mismatch caught by AI review — PASS (after provider fallback fix)
- pytest `test_ac9_targeting_mismatch` PASSED
- AI caught fashion-niche-targeting-with-finance-brief mismatch, surfaced concern matching `(?i)niche|targeting|audience`

### AC10 — AI-review fallback when AI fails — PASS (after HTTP-header fix)
- pytest `test_ac10_ai_review_fallback` PASSED
- Sent `X-Amplifier-UAT-Bypass-AI-Review: 1` header. AI review raised, fell back to mechanical-only, campaign activated on rubric strength.

### AC11 — Idempotence — PASS
- pytest `test_ac11_rubric_idempotent` PASSED
- Two `/score` calls within 5s returned byte-identical responses

### AC12 — Pre-flight UI on draft detail page — PASS (Chrome DevTools MCP)
- Logged into company dashboard as `nike.corp@gmail.com`
- Navigated to `/company/campaigns/47` (bad_minimal)
- Quality Score widget shows "NEEDS WORK" badge, score "2/100", 8 actionable feedback bullets, per-criterion grid with all 8 criteria
- Activate button has `disabled` attribute + `description="Score below 85 — fix issues to activate"`
- `list_console_messages(types=["error"])` → zero errors
- Screenshot: `task15_ac12_v2_blocked.png`

### AC13 — Audit log entry per gate run — PASS (Supabase SQL)
- Direct query against Supabase Postgres via the prod DATABASE_URL (no test code changes — used psycopg from local Python)
- Query: `SELECT id, action, created_at, details FROM audit_log WHERE action LIKE 'campaign_quality_gate%' OR action IN ('campaign_activated','campaign_flagged_caution') ORDER BY id DESC LIMIT 50`
- 50 rows in the UAT window (spec required ≥5):
  - 11× `campaign_quality_gate_blocked`
  - 16× `campaign_quality_gate_passed`
  - 17× `campaign_activated`
  - 6× `campaign_flagged_caution`
- Each row's `details` JSON has the spec-required keys: `campaign_id, score, passed, ai_review_outcome` (caution rows also include `concerns`)
- Sample payload: `{"score": 100, "passed": true, "campaign_id": 57, "ai_review_outcome": "bypassed"}`
- Evidence: `data/uat/ac13_audit.json` (50 rows)

### AC14 — Full UI lifecycle: bad → fix → activate — PASS (Chrome DevTools MCP)
- Drove company dashboard end-to-end
- Step 1: navigated to `/company/campaigns/47` (bad_minimal) — confirmed Activate disabled (per AC12)
- Step 2: PATCHed campaign 55 (`idempotence_check`) to good values via API. **PATCH applied all nested fields** — verified via `GET /api/companies/me/campaigns/55/score` returned 87/100 with payout_rates 15/15, targeting 10/10, assets 10/10, dates 5/5 (all from the PATCH payload).
- Step 3: navigated to `/company/campaigns/55` — Quality Score widget shows "PASS" badge, score 87/100, Activate button enabled
- Step 4: clicked Activate
- Step 5: page redirected to `?success=Campaign+active` within 1s
- Step 6: status badge changed from "draft" to "active", "Pause" button replaced "Activate", success banner "Campaign active" shown
- Zero console errors throughout
- Screenshots: `task15_ac14_v2_before_activate.png` (PASS state), `task15_ac14_v2_after_active.png` (active state with banner)

## Aggregated PASS rule
- All 14 ACs PASS: **YES**
- Server log error count during window: zero unhandled errors. Some logged Gemini 503 fallthrough lines (expected — Mistral picked up; the chain worked as designed).
- audit_log has ≥5 quality_gate rows: YES (50)
- Cleanup ran: YES — all 10 fixtures cancelled via `/company/campaigns/{id}/status` after the run
- UAT report file written: ✅ this file
- All evidence embedded: ✅ (3 screenshots + audit JSON + pytest output)

## What changed since the 1545 partial run

5 follow-ups landed (commits 367ef71, 68f3ba4, 240f6da, 4314e5b, 0e9f288):
1. **Fix 1 + 1b**: Gemini retry (3x exp backoff on 503/429) + full provider fallback chain (Gemini → Mistral → Groq). Mistral and Groq keys deployed to prod from user's local onboarding storage.
2. **Fix 2**: disabled-platform also surfaces as a rubric feedback bullet (cleaner UX). Did NOT need to reorder activation checks — AC5 was a different root cause.
3. **Fix 3**: PATCH endpoint applies nested fields correctly. Schema extended with `payout_rules`, `targeting`, `assets`, `budget_total`, dates.
4. **Fix 4**: cleanup script accepts IDs from JSON file even when title doesn't match prefix.
5. **Fix 5**: HTTP-header test-mode flags (`X-Amplifier-UAT-Bypass-AI-Review`, `X-Amplifier-UAT-Force-AI-Review-Result`) replace server-process env vars. Honored only for UAT test company by email match. AC8 + AC10 now runnable without restart.
6. **Bonus fix**: hard-fail criteria — zero score on payout_rates / assets_provided / targeting now vetoes activation regardless of total. Fixed AC5 boundary case (zero rates was scoring exactly 85).

## Recommendation

**Mark Task #15 done.** All 14 ACs PASS. Production behavior is verified end-to-end:
- Mechanical rubric scores 8 criteria deterministically
- AI review survives transient provider outages via 4-step fallback chain
- Hard-fail criteria veto activation when fundamentals are missing
- Audit log captures every gate decision with full payload
- UI widget renders inline on campaign detail pages
- End-to-end activate flow works (bad → fix → activate) with no console errors

Next task per STATUS.md execution order: Phase A complete → Phase C starts with **Task #18 (pytest suite)**, but per docs/STATUS.md "Tasks lacking AC blocks" list, **Task #52** (backfill AC blocks for #23-28 polish tasks) is technically before #18 if we want AC-format coverage on those before running. Confirm with user before picking next.

## Notes

- Gemini API was still mostly 503-throttled during this run (`gemini-2.0-flash-thinking-exp` model in particular). The provider fallback chain handled it gracefully — most AI-review calls succeeded via Mistral or Groq. This is exactly the behavior the new chain was designed to deliver.
- AC14's PATCH→activate path doubles as live verification of Fix 3 (nested PATCH) — the activation succeeding on score 87 proves the PATCH actually applied the nested values, otherwise the score would have stayed at the seed default.
- AC8's HTTP-header path (`X-Amplifier-UAT-Force-AI-Review-Result`) is a generally-useful test-mode pattern. Recommend extending it to other server features that have `AMPLIFIER_UAT_*` env-var test modes (matching, content_agent, billing) for faster UAT iteration.

## Cleanup confirmation

All 10 fixtures cancelled successfully (status moved to `cancelled`). No leftover UAT data on prod.
