# UAT Report — Task #14 — 2026-04-25 19:32 IST

**Result**: PARTIAL — only 2 of 14 ACs eligible to run; both passed
**Skill version**: first run (skill files committed at start of this session)
**Learnings applied**: 1 (resize browser to 1920x1080 — not exercised in this partial run since AC14 was blocked)
**--allow-prod**: no
**Total duration**: ~3 minutes

## Summary
- ACs passed: **2/14** (AC4, AC13)
- ACs failed: 0
- ACs blocked: 11 (AC1, AC2, AC3, AC5, AC6, AC7, AC8, AC9, AC10, AC11, AC14) — missing infrastructure
- ACs skipped: 1 (AC12 — needs `scripts/uat/uat_task14.py` which doesn't exist)

## Why this is PARTIAL, not PASS or FAIL

This was an Option-B exploratory run to surface bugs in the framework itself before investing in helper scripts. The skill correctly refused to run 11 ACs because:

1. **3 UAT test-mode env vars are not yet read by the code**:
   - `AMPLIFIER_UAT_INTERVAL_SEC` — `scripts/background_agent.py` and `scripts/utils/content_agent.py` ignore it; agent loop and 7-day research-cache TTL are hardcoded
   - `AMPLIFIER_UAT_BYPASS_AI` — `manager.generate()` doesn't check this env var (AC9 requires it)
   - `AMPLIFIER_UAT_FORCE_DAY` — `day_number` is computed from `unique_dates` of past drafts in `background_agent.py:292-293`, not from env (AC8 requires it)

2. **6 helper scripts in `scripts/uat/` do not exist**:
   - `seed_campaign.py` — required by Test data setup step 1
   - `reset_local_cache.py` — required by Test data setup step 2
   - `dump_research.py` — required by AC2, AC3, AC5
   - `dump_drafts.py` — required by AC7
   - `cleanup_campaign.py` — required by AC14 cleanup
   - `uat_task14.py` (pytest) — required by ACs 1, 3, 5, 6, 7, 8, 9, 10, 11, 12, 13

These are tracked as Task #48 in task-master.

## Screenshot index

| # | File | AC | Caption |
|---|------|----|---------|
| — | N/A | AC4 | Pure dict inspection — no UI rendering |
| — | N/A | AC13 | Pure code-level check — no UI rendering |

(no screenshots produced — both runnable ACs are non-UI code checks)

## ACs

### AC4 — PASS

- **Action**: `python -c "from scripts.utils.content_agent import GOAL_STRATEGY; ..."` to inspect the static mapping
- **Expected** (from spec):
  - `virality.x.hooks` contains at least one of `contrarian`, `surprising_result`, `curiosity`
  - `leads` strategies have `cta` containing `link` or `comment_link` on non-Reddit
  - `brand_awareness.x.posts_per_day == 1` AND `brand_awareness.reddit.posts_per_day < 1`
- **Actual**:
  - virality.x.hooks = `['contrarian', 'curiosity', 'surprising_result']` ✓
  - leads CTAs: x=`link_in_bio`, linkedin=`comment_link`, facebook=`link_post` (all match) ✓
  - brand_awareness: x=1, reddit=0.3 ✓
- **Evidence**:
  - File: `data/uat/ac4_run.log`
  - Log excerpt:
    ```
    AC1 (virality hooks): True — hooks=['contrarian', 'curiosity', 'surprising_result']
    AC2 (leads CTAs): True — {'x': ('link_in_bio', True), 'linkedin': ('comment_link', True), 'facebook': ('link_post', True)}
    AC6 (brand_awareness ppd): True — x=1, reddit=0.3
    ```
  - Screenshots: N/A — pure dict inspection
- **Duration**: ~1s

### AC13 — PASS

- **Action**: `python -c "from utils.content_quality import validate_content; from utils.guard import filter_disabled; ..."` — exercise both safety guards
- **Expected**:
  - `validate_content({"x": "a"*500}, [], dummy_manager)` returns `(False, [reason containing "exceeds 280"])`
  - `filter_disabled(['x','linkedin','facebook','reddit'])` returns list without `'x'`
- **Actual**:
  - `validate_content(...)` → `(False, ['x: exceeds 280 chars (500 chars after FTC disclosure)'])` ✓
  - `filter_disabled(...)` → `['linkedin', 'facebook', 'reddit']` ✓
- **Evidence**:
  - File: `data/uat/ac13_run.log`
  - Screenshots: N/A — pure code-level check
- **Duration**: ~2s

### ACs 1, 2, 3, 5, 6, 7, 8, 9, 10, 11, 14 — BLOCKED

All 11 blocked on Task #48 (helper scripts) and/or test-mode env-var reads in code.

### AC12 — SKIPPED

Needs `scripts/uat/uat_task14.py` (pytest with monkey-patching) which doesn't exist. This is the one AC where mocking is allowed; once the file is written it will be runnable independently of the env-flag work.

## Aggregated PASS rule

- All ACs pass: **NO** (only 2 of 14 ran)
- agent.log error count: N/A (no agent run executed this UAT)
- audit_log error rows: N/A (no server interaction)
- agent_research final row count: N/A
- All cleanup ran: yes — only `data/uat/*.log` files created, no orphan processes/campaigns
- Cleanup verified: nothing to clean (no agent/app started, no campaign created)

## Recommendation

**Do NOT mark Task #14 done.** Two ACs passing only proves the static mapping and the X safety guards work — does NOT prove the 4-phase content agent works in production.

Next steps:

1. **Build helper scripts** (Task #48) — delegate to `amplifier-coder`. The Task #14 Verification Procedure block in `docs/specs/batch-2-ai-brain.md` lines 612-839 specifies each script's command-line signature. ETA: 2-3 hours.
2. **Add 3 env-var reads in code** — `content_agent.py` and `background_agent.py` need to check `AMPLIFIER_UAT_INTERVAL_SEC`, `AMPLIFIER_UAT_BYPASS_AI`, `AMPLIFIER_UAT_FORCE_DAY` at the relevant points. Defaults preserve production behavior. ETA: 30 min.
3. **Re-run `/uat-task 14`** — should be able to run all 14 ACs end-to-end. ETA: ~20 min wall clock.

## Notes

- This run validated the skill's hard rules work as designed: it correctly refused to mock or fake unsupported flags, and refused to skip cleanup. The "two passing ACs" result is honest.
- AC4/AC13 are useful as **fast smoke tests** — they catch breakage of the static configuration in <5 seconds. Worth keeping as a pre-flight check before every full UAT run.
- LEARNINGS.md has 1 entry (browser resize). Not applied this run since no AC reached the Chrome DevTools MCP layer.
- Report path follows convention: `docs/uat/reports/task-14-YYYY-MM-DD-HHMM.md` so multiple runs/day don't overwrite.

## Framework observations from this first run

Captured here so they're not lost — these aren't strictly "learnings" (skill mistakes) but observations that may inform AC-FORMAT.md or LEARNINGS.md after more runs:

1. **Spec block lines 612-839 are correct** — the AC tables are well-structured, the Test-mode flags table is clear, the Aggregated PASS rule is explicit. No format bugs surfaced.
2. **Skill behavior on missing infrastructure was correct** — it stopped before running setup, surfaced the missing pieces, offered Option A vs Option B. This is exactly what Hard Rule #5 demands.
3. **The 2 runnable ACs each took <2 seconds** — pure-code ACs are extremely cheap. Worth a separate `--ac-fast` flag in future runs if we want a 5-second smoke check.
4. **Screenshot index in the report is empty for code-only ACs** — the format handled this gracefully via `screenshots: N/A — <reason>`. The hard rule about screenshots being mandatory only applies to UI-touching ACs, which is correct.
