# UAT Report — Task #71 (Wizard create-and-activate audit_log + AI review)

**Date**: 2026-04-29 23:01 IST
**Server**: https://api.pointcapitalis.com (Hostinger KVM 1, Mumbai)
**Branch**: `flask-user-app` (deployed via push to `main` + SSH systemctl restart)
**Test company**: nike.corp@gmail.com (UAT_TEST_COMPANY_EMAIL)
**Run mode**: autonomous E2E via `scripts/uat/verify_task71_72.py` — real form submissions via cookie-authenticated httpx + real Gemini/Mistral/Groq AI review + direct SQL diff against Supabase. Chrome DevTools MCP UI verification deferred to next session.

## Result: 5/5 ACs PASS ✅

| AC | Description | Result | Evidence |
|---|---|---|---|
| AC1 | Wizard activate (gate passes) writes `campaign_quality_gate_passed` + `campaign_activated` audit rows | **PASS** | Campaign 90 created, both audit rows present |
| AC2 | Wizard activate (rubric blocks) writes `campaign_quality_gate_blocked` row with `ai_review_outcome=null` | **PASS** | HTTP 422, 1 blocked row written |
| AC3 | Wizard activate (harmful guidance, real AI review) writes blocked row with `ai_review_outcome='reject'` | **PASS** | HTTP 422, AI rejected (Mistral fallback path — Gemini quota-throttled today) |
| AC4 | Wizard activate (caution forced via header) writes passed + flagged_caution + activated rows + admin_review_queue row | **PASS** | Campaign 93 — all 3 audit rows + 1 queue row |
| AC5 | Detail-page Activate path regression — still writes passed + activated rows | **PASS** | Campaign 94 — both audit rows |

## Bugs surfaced + fixed during UAT

### Bug — Wizard path didn't set `_uat_requester_email` for header gating

After the initial #71 fix shipped (commit `427540e`), AC4 failed. Root cause: `ai_review_campaign()` checks `campaign._uat_requester_email == UAT_TEST_COMPANY_EMAIL` before honoring `X-Amplifier-UAT-Force-AI-Review-Result` and `X-Amplifier-UAT-Bypass-AI-Review` headers. The API path (`routers/campaigns.py:875`) sets this attribute before calling `ai_review_campaign`. The wizard path (newly added in fix) didn't.

**Fix**: Added `campaign._uat_requester_email = company.email` before the `ai_review_campaign(campaign, request_headers=request.headers)` call in `campaign_create_submit`. Shipped as commit `31eefc3`.

After this 1-line fix, AC4 passed cleanly: `audit:['campaign_quality_gate_passed', 'campaign_flagged_caution', 'campaign_activated'], queue:1`.

## Aggregated PASS rule check

1. ✅ AC1–AC5 all PASS (AC3 manual y/n: AI returned `reject` with concerns — sensible classification of harmful guidance about fake reviews + competitor defamation)
2. ✅ `audit_log` diff before/after the run shows ≥ 8 new rows across 4 expected event types (`campaign_quality_gate_passed`, `campaign_quality_gate_blocked`, `campaign_flagged_caution`, `campaign_activated`)
3. ✅ Server logs grep for errors: clean (some `429 RESOURCE_EXHAUSTED` from Gemini are expected — provider chain fell through to Mistral)
4. ✅ All 3 created campaigns (90, 93, 94) cancelled by cleanup
5. ✅ This UAT report written

## Side issues caught

- **Gemini 1.5-flash returns 404 NOT_FOUND** on v1beta API (`'models/gemini-1.5-flash is not found ... or is not supported for generateContent'`). Filed as Task #73 (low priority — provider chain falls through to Mistral correctly, but the retry step is dead). Quick fix: replace model id with `gemini-1.5-flash-latest` or remove from chain.
- **Gemini 2.0-flash quota exhausted** today (`429 RESOURCE_EXHAUSTED` — daily request limit on free tier). Provider chain fell through to Mistral and the AI review still functioned correctly. This validates the chain in production for a third consecutive day.

## Recommendation

**Mark Task #71 done**. The fix closes the audit-trail compliance gap caught in W8 SaaS UAT 2026-04-29. Wizard activations now have full traceability matching the detail-page Activate path.

## Files

- Fixes: `server/app/routers/company/campaigns.py` (commits `427540e`, `31eefc3`)
- ACs: `docs/specs/batch-2-ai-brain.md` Task #71 section
- UAT helper: `scripts/uat/verify_task71_72.py`
- Console log: `data/uat/task71_72_console.log`
- Structured run output: `data/uat/task71_72_run_1777483608.json`
